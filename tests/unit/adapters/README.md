# Adapters (`tests/unit/adapters/`)

Unit tests for `src/adapters/` — split into `driven/` (infrastructure adapters: database, fetch,
browser) and `ui/` (Vue-facing adapters: components, composables, stores).

## Directory Structure

### Directories

- `driven/`
- `ui/`

