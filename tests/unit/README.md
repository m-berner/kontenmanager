# Unit (`tests/unit/`)

Vitest unit tests. Mirrors the hexagonal layout of `src/` 1:1 (see `tests/README.md` for the full
tree and conventions).

## Directory Structure

### Directories

- `adapters/`
- `app/`
- `domain/`
- `support/`

### Files

- `architecture.test.ts`
- `backupImportRefresh.test.ts`

